# VeraCrypt Format.exe - Windows volume formatting tool
# TODO: Phase 7 - Windows build implementation

if(NOT WIN32)
    return()
endif()

message(STATUS "Windows Format target - Phase 7 stub")
add_library(veracrypt_format INTERFACE)
