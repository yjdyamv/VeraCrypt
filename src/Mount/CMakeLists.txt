# VeraCrypt.exe - Windows GUI main application
# TODO: Phase 7 - Windows build implementation

if(NOT WIN32)
    return()
endif()

message(STATUS "Windows Mount target - Phase 7 stub")

add_library(veracrypt_mount INTERFACE)
