# VeraCryptExpander.exe - Windows volume expansion tool
# TODO: Phase 7 - Windows build implementation

if(NOT WIN32)
    return()
endif()

message(STATUS "Windows ExpandVolume target - Phase 7 stub")
add_library(veracrypt_expander INTERFACE)
